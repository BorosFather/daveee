{
  "scripts": {
    "start": "npx lite-server",
    "json":"npx json-server --watch database.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.0",
    "lite-server": "^2.6.1"
   
  }
}
